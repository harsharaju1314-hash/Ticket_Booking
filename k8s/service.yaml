apiVersion: v1
kind: Service
metadata:
  name: eventflow-backend-service
  namespace: default
  labels:
    app: eventflow-backend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: eventflow-backend
